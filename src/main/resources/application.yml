spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/restapi1
    driver-class-name: org.mariadb.jdbc.Driver
    username: restapi1
    password: 1111
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace